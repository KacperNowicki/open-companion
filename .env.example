# ChatGPT Plus OAuth client ID (PKCE public client)
# Optional override for the built-in ChatGPT OAuth public client ID.
# Copy this file to `.env` only if you want to test a different OAuth app locally.
# If unset, OpenCompanion falls back to the published public client used for ChatGPT login.
CHATGPT_OAUTH_CLIENT_ID=
